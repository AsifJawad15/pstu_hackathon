global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/slo-rules.yaml

scrape_configs:
  - job_name: aegis-regional-api
    static_configs:
      - targets: ["api:8080"]
  - job_name: aegis-python-optimizer
    static_configs:
      - targets: ["optimizer:8090"]
